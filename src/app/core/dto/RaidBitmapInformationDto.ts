import { ValueDto } from './ValueDto';

export interface RaidBitmapInformationDto {
  chunkSize: ValueDto;
  existingPages: ValueDto;
  file: ValueDto;
  pagesSize: ValueDto;
  totalPages: ValueDto;
}
